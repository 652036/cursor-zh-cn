## 改动说明

说明本次改动解决的问题和实现方式。

## 验证

- [ ] 已运行 `python scripts/build_js.py --check`
- [ ] 已运行 `python -m unittest discover -s tests -v`
- [ ] 已运行 `node --check cursor-zh.js`
- [ ] 已在相关操作系统和 Cursor 版本中验证（如适用）

## 提交确认

- [ ] 改动只包含与本 Pull Request 有关的内容
- [ ] 自动生成的 `cursor-zh.js` 已同步更新（如适用）
- [ ] 日志、截图和提交内容不包含敏感信息
